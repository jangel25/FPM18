<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AppCocacolaNayMobiV2.Views.Planeaciones.ViEvaPlanSubtemasItem"
             xmlns:vm="clr-namespace:AppCocacolaNayMobiV2.ViewModels.Planeaciones;"
             xmlns:numeric="clr-namespace:Syncfusion.SfNumericUpDown.XForms;assembly=Syncfusion.SfNumericUpDown.XForms"
             Title="Nuevo tema">

    <ScrollView>
        <StackLayout
        VerticalOptions="StartAndExpand"
        Padding="20">
            <Label x:Name="lblPlaneacion"/>

            <Label Text="Descripcion del subtema" />
            <Entry Text="{Binding eva_planeacion_subtemas_item.DesSubtema}" x:Name="txtReferenciaNorma"/>

            <Button Text="Guardar"  Command="{Binding SaveCommand}"/>
            <Button Text="Cancelar" Command="{Binding CancelCommand}"/>
        </StackLayout>
    </ScrollView>
</ContentPage>